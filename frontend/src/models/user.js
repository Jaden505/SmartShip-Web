export default class User {
    constructor(name, email, password, shipID) {
        this.username = name;
        this.email = email;
        this.password = password;
        this.shipID = shipID;
    }
}